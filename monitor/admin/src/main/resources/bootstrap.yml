server:
  port: ${SERVER_PORT:8022}
spring:
  application:
    name: admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:10.4.14.24}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:10.4.14.24}:${REGISTER_PORT:8848}
        file-extension: yml
